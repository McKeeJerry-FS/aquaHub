<Project>
  <!-- Override the target that checks for resource files -->
  <Target Name="GenerateSatelliteAssemblies" />
  
  <!-- Explicitly set that we have no resx files -->
  <PropertyGroup>
    <GenerateResourceUsePreserializedResources>false</GenerateResourceUsePreserializedResources>
  </PropertyGroup>
</Project>
